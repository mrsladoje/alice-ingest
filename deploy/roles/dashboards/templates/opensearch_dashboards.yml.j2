# {{ ansible_managed }}

server.host: "127.0.0.1"
server.port: {{ dashboards_internal_port }}

opensearch.hosts: {{ dashboards_opensearch_hosts | to_json }}

opensearchDashboards.branding:
  applicationTitle: "ALICE Cockpit"

vis_type_vega.enableExternalUrls: true
