# {{ ansible_managed }}
#
# What the worker's boot-time self-heal needs that is not already in the index
# template this role renders beside it. On a worker, fluent-bit.service loads
# this file beside node.env.

# A literal, not a variable. ism.sh.j2 creates this policy under this exact name
# and verify_detection.py asserts it. See the note in defaults/main.yml.
ALICE_LOCAL_RETENTION_POLICY=alice-application-local-retention
